export enum DialogType {
    Alert,
    Error,
    Ok,
    OkCancel,
    YesNoCancel,
    Sucess,
    SucessSave,
    ErroSave,
    SucessDelete,
    ErrorDelete,
    Process
}

export enum DialogResult {
    Abort,
    Cancel,
    Ignore,
    No,
    None,
    OK,
    Retry,
    Yes
}